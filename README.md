# LedgerValuationEngine
LedgerValuationEngine is an enterprise-grade, event-sourced transaction processor built in Java 25. It strictly enforces Domain-Driven Design and CQRS to ingest sequential data streams and deterministically calculate real-time Account Value. The engine guarantees complete data integrity and auditability through immutable, append-only event logs
